[project]
name = "cc-session-explorer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "commit-context",
]

[tool.uv.sources]
commit-context = { path = "commit-context", editable = true }
